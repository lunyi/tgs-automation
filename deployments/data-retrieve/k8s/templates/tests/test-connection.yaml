apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-retrieve.fullname" . }}-test-connection"
  labels:
    {{- include "data-retrieve.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-retrieve.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
