kind: pipeline
type: kubernetes
name: default

clone:
  retries: 5
  
steps:
- name: build-and-test
  image: golang:1.21.1
  commands:
  - ls -al
  - pwd
  - hostname
  - go mod download
  - go build ./...
  - go test ./...
  - go build -o main ./cmd/expired_domain

- name: docker-build
  image: plugins/docker
  privileged: true
  settings:
    debug: true
    registry: registry.hub.docker.com
    repo: pgtgs/expired_domain
    tags: latest
    dockerfile: dockerfiles/Dockerfile.expired_domain
    username: pgtgs
    password: dckr_pat_9bLt9kNG5MmSQs-m-n6oYTcjwJ4

trigger:
  branch: 
  - main
#   event:
#   - push
